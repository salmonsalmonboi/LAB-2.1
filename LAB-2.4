# LAB-2.4
66543206088-7 นายศุภกร ศิริเมืองมูล

#include <stdio.h>

void explode( char str1[], char splitter, char str2[][10], int *count ) ;

int main() {
	char out[ 20 ][ 10 ] ;
	int num ;
	int i = 0;
	
	explode( "I/Love/You", '/' ,out, &num) ;	
	
	printf("Input: I/Love/You \n", out) ;
	printf("--------------------------------\n") ;
    for (i = 0; i < num; i++) {
        printf("[%d] = \"%s\"\n", i, out[i]) ;
    }//end for
    printf("count = %d", num) ;
 	
	return 0 ;
}//end function

void explode( char str1[], char splitter, char str2[][10], int *count ) {
	*count = 0 ;
	int i, j = 0 ;
	
	//Check if the current character is a splitter(/) or not.
	while(str1[i] != '\0') {
		if (str1[i] == splitter) {
			str2[ *count ][j] = '\0';
			
			(*count)++;
			j = 0;
				
		} else {
			//Counter for number of text that can be splitter
			str2[*count][j] = str1[i];
            j++;
		}
			i++;
	}//end 
	str2[*count][j] = '\0';
		(*count)++;
}//end function
